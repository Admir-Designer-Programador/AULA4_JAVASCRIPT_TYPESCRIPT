// 3. Faca um programa que imprima os numeros impares de 0 a 50;

// For (Inicialização; condição; contador +=2)
// For (Inicialização; condição; contador = contador + 2)

for (let contador = 1; contador <= 100; contador += 2){
    console.log(contador)
}

